    """User notification"""
    type Notification {
        notificationId: ID!
        userId: ID!
        type: NotificationType!
        title: String!
        body: String!
        data: JSON
        channel: NotificationChannel!
        isRead: Boolean!
        readAt: DateTime
        createdAt: DateTime!
    }
