<?xml version="1.0" encoding="utf-8"?>
<!--
  Nammerha Network Security Config
  ═══════════════════════════════════════════════════════════════════════════
  Production: HTTPS only (cleartext forbidden)
  Development: Allow cleartext ONLY for local emulator/simulator addresses
  ═══════════════════════════════════════════════════════════════════════════
-->
<network-security-config>
    <!-- Default: Block all cleartext traffic -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>

    <!-- Dev-only: Allow cleartext for local development servers -->
    <domain-config cleartextTrafficPermitted="true">
        <!-- Android emulator loopback -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <!-- iOS simulator / desktop -->
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
